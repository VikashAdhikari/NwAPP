name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.com/VikashAdhikari }}
          password: ${{ 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 }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          tags: ghcr.io/${{ github.com/VikashAdhikari }}/hello-world:latest
